pipeline {
  agent any
  stages {
    #stage('Approval') {
    #  steps {
    #    input "Approve this change?"
    #  }
    #}
    stage('Build') {
      steps {
        echo 'I am building'
      }
    }
  }
}